// sintaxis similar CSS de querySelectorAll 

// te retorna todos los elementos que cumplan el selector 

const card = document.querySelectorAll('.card');
console.log(card);

// JS genera un NodeList usando los div y class

//  in un doc no existe y este se retorna a la console, mustra un arreglo vacio


