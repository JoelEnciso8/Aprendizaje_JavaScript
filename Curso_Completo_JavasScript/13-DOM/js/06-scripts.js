const encabezado = document.querySelector('.contenido-hero h1').textContent
// console.log(encabezado);

// Accediendo al texto desde HTML 

// console.log(encabezado.innerText); // si en el CSS = visibility = hidden no lo encontrara mientras que 
// console.log(encabezado);  // textContent si puede ser visto
// console.log(encabezado.innerHTML); //se trae el HTML


// manipulando el DOM

const imagen = document.querySelector('.card img');
