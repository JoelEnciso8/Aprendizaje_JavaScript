// Metodos para los Strings o cadena de texto
const producto = "Monitor 20\'"
console.log(producto);

// conocer la cantidad de letras de la cadena de texto (Strings)
console.log(producto.length);

// IndexOf nos ayuda a localizar la posicon del array en cuanto a lo que requerimos que nos aparezca en consola
console.log(producto.indexOf('Monitor'));


// Includes nos valida si el array estan incluido en el elemento, siendo si marcara True siendo no False
console.log(producto.includes('Monitor'));
console.log(producto.includes('Tablet'));
