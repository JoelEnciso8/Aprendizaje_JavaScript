const producto = "Monitor 20 Pulgadas";
// para que las validacione que requieren minusculas o minusculas se usan los siguientes metodos
console.log(producto.toUpperCase());
console.log(producto.toLowerCase());

// Los numeros NO SON STRINGS
const price = 200;

// ToString convert the numbers to strings

console.log(price.toString());
